[tool.poetry]
name = "Cocktailmaker"
version = "1.1.0"
description = "A Python and Qt based App for a Cocktail Maker on a Raspberry Pi"
authors = ["Andre Wohnsland <Andre_Wohnsland@web.de>"]
license = "MIT"

[tool.poetry.dependencies]
python = "3.6"
PyQt5 = "^5.15.2"
requests = "^2.25.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
