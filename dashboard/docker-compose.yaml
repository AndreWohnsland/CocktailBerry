version: '3'

services:
  backend:
    image: cocktail-dashboard-backend
    container_name: cocktail-dashboard-backend
    restart: always
    build: backend
    ports:
      - 8080:8080
    volumes:
      - ./storage:/app/storage
